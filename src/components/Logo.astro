---
/**
 * Logo Component
 * Isometric "A" logo with light/dark variants
 * TODO: Replace with actual Anouar Boost isometric logo
 */

interface Props {
    variant?: 'dark' | 'light';
    width?: number;
}

const { variant = 'dark', width = 40 } = Astro.props;
const fillColor = variant === 'light' ? '#FFFFFF' : '#2B2E34';
---

<!-- Placeholder logo - replace with actual isometric A logo -->
<svg width={width} height={width} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
    <!-- Isometric cube representation - simplified placeholder -->
    <!-- Bottom left cube -->
    <path d="M12 28L18 24L24 28L18 32Z" fill={fillColor} opacity="0.8"></path>
    <!-- Bottom right cube -->
    <path d="M24 28L30 24L36 28L30 32Z" fill={fillColor} opacity="0.8"></path>
    <!-- Top cube (Electric Blue accent) -->
    <path d="M18 18L24 14L30 18L24 22Z" fill="#007BFF"></path>
    <!-- Connecting edges -->
    <path d="M18 24V32M24 28V20M30 24V32" stroke={fillColor} stroke-width="1" opacity="0.4"></path>
</svg>
